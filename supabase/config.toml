# supabase/config.toml (minimal, current format)

project_id = "uezrpgdbeemoqrxuzaln"   # <-- your project ref

[api]
port = 54321
schemas = ["public", "graphql_public"]
extra_search_path = ["public", "extensions"]

[db]
port = 54322
major_version = 15

[studio]
port = 54323

[auth]
site_url = "http://127.0.0.1:3000"
additional_redirect_urls = ["http://127.0.0.1:3000"]
jwt_expiry = 3600

# Functions config must be a table now (older configs used a bool)
[functions]
# (optional) keep as an empty table or add per-function blocks later
[functions.verify_jwt]
# import_map = "supabase/functions/import_map.json"
